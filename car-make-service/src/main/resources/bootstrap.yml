spring:
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: config-server
      password: user
      username: user
      enabled: true
  application:
    name: car-make-server